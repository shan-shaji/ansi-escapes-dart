name: ansi_escapes
description: ANSI escape codes for manipulating the terminal
version: 2.0.0
homepage: https://github.com/shan-shaji/ansi-escapes-dart

environment:
  sdk: '>=2.12.0 <4.0.0'

dev_dependencies:
  test: ^1.16.0

dependencies:
  lints: ^6.0.0
